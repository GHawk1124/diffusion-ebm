[project]
name = "diffusion-ebm"
version = "0.1.0"
description = "THRML joint sampler for masked diffusion language models"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "thrml>=0.1.3",
    "jax[cuda12]>=0.4.30",
    "jaxtyping>=0.2.23",
    "equinox>=0.11.2",
    "torch>=2.3.0",
    # Pin to 4.x: the MDLM trust_remote_code files (kuleshov-group/mdlm-owt) use
    # the legacy `_tied_weights_keys` API; transformers 5.x renamed this to
    # `all_tied_weights_keys` and crashes when loading MDLM otherwise.
    "transformers>=4.40,<5",
    "huggingface-hub>=0.23.0",
    "networkx>=3.2",
    "numpy>=1.26",
    "datasets>=2.20",
    "matplotlib>=3.8",
    "ipykernel>=6.29",
    "jupyter>=1.0",
    "tqdm>=4.66",
    "einops>=0.7",
    "flash-attn>=2.7,<3",
    # Build deps that flash-attn expects to be present in the env at install time
    # (we use no-build-isolation, so its setup.py reads them from .venv).
    "ninja>=1.11",
    "packaging>=24",
    "setuptools>=70",
    "wheel>=0.43",
    "marimo>=0.23.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/diffusion_ebm"]

# Pin torch to CUDA 12.4 wheels.  PyPI's default torch picks the newest CUDA
# build (cu130 at time of writing) which requires NVIDIA driver >=580; cu124
# works with driver >=525 and pairs with the JAX [cuda12] runtime.
[[tool.uv.index]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cu124" }
# Pin flash-attn to Dao-AILab's pre-built wheel for torch 2.6 + cu12 + cp312
# with cxx11_abi=FALSE.  Source-building flash-attn from PyPI's sdist
# produces a .so linked with cxx11_abi=1 but torch 2.6.0+cu124 from
# PyTorch's index ships with cxx11_abi=0, so the source-built flash-attn
# fails at import with undefined `c10::Error::Error(... std::__cxx11::string)`.
# Using the matched pre-built wheel skips the 10-min compile and avoids the
# ABI mismatch entirely.
flash-attn = { url = "https://github.com/Dao-AILab/flash-attention/releases/download/v2.7.4.post1/flash_attn-2.7.4.post1%2Bcu12torch2.6cxx11abiFALSE-cp312-cp312-linux_x86_64.whl" }

# PyTorch's cu124 wheels use the plain `linux_x86_64` platform tag rather than
# `manylinux_*`, which uv treats as system-specific by default.  Declaring the
# required environment lets uv accept those wheels on x86_64 Linux hosts.
[tool.uv]
required-environments = [
    "sys_platform == 'linux' and platform_machine == 'x86_64'",
]
# flash-attn must be built against an already-installed torch.  Disabling
# build isolation lets its setup.py see torch from .venv during compilation.
no-build-isolation-package = ["flash-attn"]
